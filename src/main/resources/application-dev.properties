server.port=8000
spring.datasource.url=jdbc:postgresql://localhost:5432/dico
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}


# Gebruik 'create' voor ontwikkeling om database en code synchroon te houden.
# Let op: dit verwijdert alle data bij elke herstart.
spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=true
spring.datasource.driver-class-name=org.postgresql.Driver

# Disable Open-in-View to prevent performance issues and enforce good data access patterns
spring.jpa.open-in-view=false

# --- JWT Secret ---
jwt.secret=bXlzdXBlcnNlY3JldGtleWZvcmRpYWJldGljY29ubmVjdGFwcGxpY2F0aW9uMjAyNA==
jwt.expiration=3600000

# --- Encryption key for sensitive data ---
dico.encryption.key=${DICO_ENCRYPTION_KEY}


# Logging
logging.level.root=DEBUG
logging.level.org.springframework=INFO
logging.level.com.example_jelle.backenddico=DEBUG

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# --- Mail Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dicodiabetesconnect@gmail.com
spring.mail.password=DicoNovi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Google OAuth2 ---
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}


# --- LibreView API Configuration ---
libreview.baseUrl=https://api-eu.libreview.io
libreview.product=llu.android
libreview.version=4.17.0

# --- Security Cookie Configuration ---
security.cookie.name=lv_jwt
security.cookie.accountIdName=lv_account_id
# IMPORTANT: Set to 'true' in a production environment (HTTPS)
# Set to 'false' for local development (HTTP)
security.cookie.secure=false
security.cookie.httpOnly=true
security.cookie.sameSite=Lax
